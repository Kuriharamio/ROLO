<launch>

    <arg name="project" default="rolo_ros2"/>

    <!--- Run Rviz-->
    <node pkg="rviz" type="rviz" name="$(arg project)_rviz" args="-d $(find rolo_ros2)/rviz/rolo_ros2.rviz" />

</launch>
